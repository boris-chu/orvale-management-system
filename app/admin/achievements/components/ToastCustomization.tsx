'use client';

import React from 'react';
import { Alert } from '@mui/material';

export default function ToastCustomization() {
  return (
    <div>
      <Alert severity="info">
        Toast customization interface coming soon! This will allow you to configure notification animations, sounds, and styles.
      </Alert>
    </div>
  );
}